//                      -*-java-*-
package tt;
path /channel;

/*
 * A user HTML+JS channel is only indirectly related to an XMPP
 * channel.
 */
class Channel
      version 1
{
    /*
     * Unique user channel identifier is always a randomized string,
     * input to the appengine Channel API.
     */
    *unique String identifier;
    /*
     * Channel token generated by appengine API
     */
    String token;
}
